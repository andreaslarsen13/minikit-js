import { ClientContent } from '@/components/ClientContent';

const Home = async () => {
  return (
    <div className="bg-white text-black min-h-full">
      <ClientContent />
    </div>
  );
};

export default Home;
